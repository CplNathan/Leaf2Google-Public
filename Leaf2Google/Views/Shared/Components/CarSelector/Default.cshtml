@model List<string?>

@{
    @if (Model != null)
    {
        @Html.DropDownList("Select Gender", Model.Select(item => new SelectListItem{Text = item, Value = item, Selected = Model.IndexOf(item) == 0}), new { @class = "rounded" })
    }
}