@{
    <div class="toast-container position-fixed bottom-0 end-0 p-3" style="z-index:2000;" id="toaster">
    @foreach (ToastViewModel toast in ViewBag.Toasts ?? new List<ToastViewModel>())
        {
            @await Html.PartialAsync("Toaster/ToastPartial", toast);
        }
    </div>
}